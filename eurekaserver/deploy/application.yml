eureka:
    instance:
        hostname: peer1
        client:
            serviceUrl:
              defaultZone: http://peer1/eureka/
            register-with-eureka: true
            fetch-registry: true