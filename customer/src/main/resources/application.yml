# Core application configuration
server:
  port: 8080
spring:
  application:
    name: Customer

# Cloud configuration
eureka:
  client:
    enabled: true
    serviceUrl:
      defaultZone: http://peer1:8761/eureka/

feign:
  name: stores